<h1>All Categories</h1>

<% @categories.each do |category| %>
  <%= category.title %>
  <%= link_to "Edit", edit_category_path(category) %>
  <%= link_to "Delete", category_path(category), method: "delete", data: {confirm: "Really delete the article?" %>
<% end %>
